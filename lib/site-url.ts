/**
 * Базова адреса сайту — одне джерело правди для sitemap.xml, robots.txt
 * і metadataBase (з нього будуються абсолютні URL для Open Graph).
 *
 * Порядок пріоритету:
 *   1. NEXT_PUBLIC_SITE_URL — якщо адресу задано вручну;
 *   2. URL — Netlify підставляє її на етапі збірки, враховуючи власний домен,
 *      тож після перенесення домену нічого правити в коді не треба;
 *   3. запасний варіант для локальної розробки.
 *
 * Значення читається під час збірки (метадані, sitemap і robots генеруються
 * статично), тому змінна без префікса NEXT_PUBLIC_ тут цілком доречна.
 */
const RAW = process.env.NEXT_PUBLIC_SITE_URL || process.env.URL || "https://lubny-cprpp.netlify.app"

/** Без кінцевого слеша, щоб не з'являлися подвійні // у згенерованих URL */
export const SITE_URL = RAW.replace(/\/+$/, "")
